{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run-api",
            "type": "shell",
            "command": "uv run uvicorn api.main:app --reload --port 8000"
        },
        {
            "label": "lint",
            "type": "shell",
            "command": "uv run ruff check . && uv run ruff format ."
        }
    ]
}